{
  "name": "rbpmn-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Source for the two rbpmn UI documents: the read-only instance inspector and the model+manifest editor. Built into crates/rbpmn-ui/assets/, which Rust inlines into self-contained HTML.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test 'test/*.test.mjs'"
  },
  "dependencies": {
    "bpmn-auto-layout": "^1.3.0",
    "bpmn-js": "^18.22.1",
    "bpmn-moddle": "^10.1.0"
  },
  "devDependencies": {
    "vite": "^8.2.1"
  },
  "license": "(MIT OR Apache-2.0)"
}
