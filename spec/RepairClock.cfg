\* The shipped claim (RepairClock.tla): a timer a repair moved is never fired
\* before its due, because the claim re-checks `due_at <= now()` under the lock.
CONSTANTS
    Kinds = {"duration", "cycle"}
    Period = 2
    MaxTime = 4
    IgnoreDue = FALSE

SPECIFICATION Spec

INVARIANTS
    TypeOK
    NeverFiresEarly

PROPERTIES
    RepairNeverMovesATimerEarlier
