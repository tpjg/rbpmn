\* Trust the plan's DUE verdict across the archive gap instead of re-applying
\* it under the row lock. EXPECTED to violate OnlyDueRecordsDeleted: an
\* operator switching a key back to forever() mid-sweep would not stop the
\* in-flight batch.
CONSTANTS
    Instances = {r1, r2}
    Positions = {1, 2, 3}
    Sweepers = {s1, s2}
    FloorFromPlan = FALSE
    RecheckDue = FALSE

SPECIFICATION Spec

INVARIANTS
    TypeOK
    OnlyDueRecordsDeleted
