\* The shipped protocol: teardown withdraws the timers of every token it reaps.
CONSTANTS
    Nodes = {n1, n2}
    Tokens = {k1, k2}
    Timers = {tm1, tm2}
    NoToken = NoToken
    NoTimer = NoTimer
    BuggyTeardown = FALSE

SPECIFICATION Spec

INVARIANTS
    TypeOK
    ArmedTimersHaveLiveTokens
    NeverFiredADanglingTimer
