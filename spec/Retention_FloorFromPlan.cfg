\* The mistake delete_records warns against: advance the floor from the
\* PLANNED set rather than from what `skip locked` actually deleted. EXPECTED
\* to violate FloorIsSomethingDeleted — readers truncated for nothing.
CONSTANTS
    Instances = {r1, r2}
    Positions = {1, 2, 3}
    Sweepers = {s1, s2}
    FloorFromPlan = TRUE
    RecheckDue = TRUE

SPECIFICATION Spec

INVARIANTS
    TypeOK
    FloorIsSomethingDeleted
