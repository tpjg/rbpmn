\* Expects a VIOLATION: the claim's re-check without `due_at <= now()`. TLC
\* finds a timer that came due after the freeze stamped its clock, picked
\* before the freeze committed — the scheduler still reads the instance as
\* active — moved past now by the repair, and fired before its due.
CONSTANTS
    Kinds = {"duration", "cycle"}
    Period = 2
    MaxTime = 4
    IgnoreDue = TRUE

SPECIFICATION Spec

INVARIANTS
    NeverFiresEarly
