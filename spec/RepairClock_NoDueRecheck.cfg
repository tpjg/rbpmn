\* Expects a VIOLATION: the claim's re-check without `due_at <= now()`. TLC
\* finds the cycle occurrence picked while due at the freeze's own instant,
\* stepped past now by the repair, and fired before it.
CONSTANTS
    Kinds = {"duration", "cycle"}
    Period = 2
    MaxTime = 4
    IgnoreDue = TRUE

SPECIFICATION Spec

INVARIANTS
    NeverFiresEarly
