\* The rejected sketch at the real row arity. EXPECTED to deadlock: without
\* it, LockOrderAllRows.cfg would be a config that cannot fail, and its small
\* state count could be mistaken for coverage.
CONSTANTS
    Nodes = {n1, n2}
    Instances = {i1}
    NoOne = NoOne
    HistoricalTimerOrder = TRUE

CONSTANT RowOrder <- AllRows

SPECIFICATION Spec

INVARIANTS
    TypeOK
